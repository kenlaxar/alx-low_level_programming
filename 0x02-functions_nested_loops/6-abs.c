#include "main.h"

/**
 * _abs - computes absolute value of interger
 * @t: interger to be computed
 * Return: the absolute value
 */
int _abs(int)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
